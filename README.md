# Study HTML Maker

把貼上的文字、Markdown、HTML、PDF、DOCX 文件整理成容易閱讀和複習的單頁 HTML 學習筆記。

## 作品目標

這個專案展示的是「整合現有強大工具，做出可用的學習工具」：

- 使用 `pypdf` 讀取 PDF 文字。
- 使用 `python-docx` 讀取 Word `.docx`。
- 使用 Python 內建 HTTP server 提供貼上文字與上傳文件 API。
- 使用原生 HTML/CSS/JavaScript 完成貼上輸入、拖放上傳、站內預覽、下載、列印。

## 功能

- 直接貼上複製的文字並產生 HTML。
- 上傳 `.txt`、`.md`、`.html`、`.pdf`、`.docx`。
- 自動抽取文字並重組成 3-5 個主要章節。
- 為每個章節產生「核心結論」引用區塊。
- 將零散句子整理成短條列，並只加粗少量關鍵字。
- 偵測比較型內容時產生表格，降低閱讀負擔。
- 產生可獨立保存的 HTML 筆記。
- 支援站內預覽、HTML 原始碼查看、抽取文字查看、下載與列印。

## 執行

本機 Codex 環境可以直接用：

```bash
./run-local.sh
```

一般 Python 環境請先安裝套件：

```bash
python3 -m pip install -r requirements.txt
```

再啟動網站：

```bash
python3 app.py
```

打開瀏覽器：

```text
http://127.0.0.1:5177
```

## 可展示給教授的重點

這不是單純套模板，而是把文字貼上、文件解析、內容整理、HTML 生成、前端預覽整合成一個完整流程。PDF 和 Word 解析引用成熟開源套件，自己的部分則負責資料流設計、整理邏輯、乾淨的使用者介面和可下載的學習成果。

整理邏輯採用「視覺減負」與「邏輯重組」：

- 一頁筆記限制主要章節數，避免資訊過載。
- 使用核心結論、短條列、粗體焦點建立閱讀層級。
- 用箭頭和簡短句取代冗長連接詞。
- 遇到比較、優缺點、風險與適用場景時改用表格。

## 下一步可擴充

- 加入 OCR，支援掃描圖像型 PDF。
- 串接 AI API，把快速重點改成真正的摘要和問答。
- 加入筆記分類、標籤、搜尋。
- 儲存歷史紀錄，做成個人學習資料庫。
